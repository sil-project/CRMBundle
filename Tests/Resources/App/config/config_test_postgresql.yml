imports:
    - { resource: config.yml }

# Doctrine Configuration
doctrine:
    dbal:
        driver: pdo_pgsql
        host: 127.0.0.1
        port: 5432
        dbname: travis
        user: travis
        password: ~
        charset: UTF8
    orm:
        auto_mapping: true
