{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}
    {% if object.customer %}
        {{ 'librinfo.label.isCustomer'|trans }}
        {% if object.supplier %}
            / {{ 'librinfo.label.isSupplier'|trans }}
        {% endif %}
    {% elseif object.supplier %}
        {{ 'librinfo.label.isSupplier'|trans }}
    {% endif %}
{% endblock %}