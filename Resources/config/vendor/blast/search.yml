fos_elastica:
    indexes:
        global:
            use_alias: true
            index_name: "%blast_search.global_index_alias%__%kernel.environment%"
            finder: ~
            types:

                city:
                    properties:
                        country_code: ~
                        zip: ~
                        city: ~
                    persistence:
                        driver: orm
                        model: "%sil.model.crm_city.class%"
                        listener:
                            defer: true
                        finder: ~
                        elastica_to_model_transformer:
                            ignore_missing: true
blast_search:
    templates:
        -
            class: "%sil.model.crm_city.class%"
            template: SilCRMBundle:Search/Results:City.html.twig
