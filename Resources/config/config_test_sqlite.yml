imports:
    - { resource: config.yml }

# Doctrine Configuration
doctrine:
    dbal:
        driver: pdo_sqlite
        database_path: "%kernel.root_dir%/var/data/data_test.sqlite"
        charset: UTF8
    orm:
        auto_mapping: true
